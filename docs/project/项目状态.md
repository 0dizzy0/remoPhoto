# remoPhoto 项目状态

更新时间：2026-06-27

## 当前结论

项目已经具备个人开源项目首个 alpha 的功能基础，但还不建议直接发包。发布前应先完成：

1. 配置 release 签名，产出 signed APK/AAB。
2. 确认剩余本地 Git 改动并完成发布说明。
3. 执行一次真机 smoke test 并记录结果。

详细发布清单见[首个 Alpha Release 计划](../releases/08_首个AlphaRelease计划.md)。

本轮已完成文档目录归一、GitHub 入口文档、核心测试用例设计、Lint 告警治理、版本号更新、MIT 许可证和 Room schema 导出。Release 签名与真机回归仍未完成。

## 基本信息

| 项目 | 当前值 |
| --- | --- |
| 包名 | `com.remophoto` |
| 平台 | Android |
| minSdk / targetSdk | 29 / 35 |
| 当前版本配置 | `versionCode = 3`, `versionName = 0.1.0-alpha.3` |
| 架构 | MVVM + Repository + UseCase + 手动依赖注入 |
| UI | Jetpack Compose + Material 3 |
| 数据库 | Room，当前 schema version 4 |
| 图片加载 | Coil，支持 GIF/WebP |
| 本地仓库访问 | SAF 持久化目录授权 |
| 远程仓库 | HTTP Server + mDNS，SMB 后延 |

## 当前能力

### 本地仓库

- 添加、删除和重新扫描本地图片仓库。
- 使用 SAF 授权访问 Android 10+ 文件目录。
- 支持 PNG、JPG/JPEG、GIF、WebP、BMP。
- 子目录自动映射为层级相册。
- 大仓库扫描已引入 WorkManager、磁盘 spool、事务切换和阶段化进度。

### 相册与图片浏览

- 相册列表支持网格/列表展示、分页、相册排序和名称模糊定位。
- 相册内图片支持网格/列表展示和排序。
- 全屏浏览支持滑动、点击区域、音量键、鼠标滚轮、缩放和自动播放。
- 分类支持创建、编辑、删除和批量关联相册。

### 远程仓库

- 本设备可通过局域网 HTTP 服务共享本地相册。
- 支持 mDNS 自动发现和手动 IP 添加。
- 远程相册元数据可同步到本地 Room 缓存。
- 远程相册支持分页、封面、分类关联和离线状态处理。
- 远程仓库仅面向可信局域网，不承诺公网访问安全。

### 设置与维护

- 支持主题、深色背景类型、高对比度、排序、分页数量、播放间隔等设置。
- 支持数据库导入导出；远程连接元信息可导出，凭据不导出。
- 远程缩略图和原图缓存可统计和清理。
- 关键路径使用 `AppLogger` 记录可观测日志。

## 质量状态

| 检查项 | 最近结果 | 判断 |
| --- | --- | --- |
| `:app:testDebugUnitTest` | 2026-06-27 通过，3/3 | 覆盖较薄 |
| `:app:assembleDebug` | 2026-06-27 通过 | 达标 |
| `:app:assembleRelease` | 2026-06-27 通过 | 可构建，产物仍未签名 |
| `:app:lintDebug` | 2026-06-27 通过，`No issues found` | 达标；含 2 项局部、带原因的例外 |
| Kotlin/KSP 编译告警 | 11 条 Android API 弃用提示；Room schema 提示已消除 | 不属于 Android Lint，本轮不阻塞 |
| release 产物 | `app-release-unsigned.apk` | 未签名，alpha 阻塞 |
| Android 仪器测试 | 暂无 | alpha 不阻塞，后续补齐 |
| Room schema 导出 | v4 已生成到 `app/schemas` | 达标，后续版本持续归档 |

## GitHub 上传准备状态

| 项目 | 当前状态 | 后续动作 |
| --- | --- | --- |
| 根目录 README | 已补齐 | 发布后补真实截图和安装包链接 |
| 文档目录 | 已统一到 `docs/` 并按职责分类 | 后续变更遵循文档中心维护规则 |
| 贡献与安全说明 | 已补齐 `CONTRIBUTING.md`、`SECURITY.md` | 启用仓库后配置私密漏洞报告 |
| Issue / PR 模板 | 已补齐 | 上传后验证模板显示和标签配置 |
| 更新日志 | 已建立 `CHANGELOG.md` | 每次 Release 前补用户可见变更 |
| `.gitignore` | 已覆盖构建、签名、IDE 和本地 AI 工具目录 | 提交前继续执行忽略项审计 |
| 开源许可证 | MIT License | 发布前继续核对第三方依赖声明 |
| Git 远程仓库 | 当前未配置 | 创建 GitHub 仓库后配置 `origin` |
| CI | 尚未配置 | Alpha 可先使用本地门禁，后续增加 GitHub Actions |

## 已验证样本

已验证过的手工样本包括：

- Debug APK 在真机上冷启动成功。
- 本地仓库扫描和相册浏览可用。
- 两台 Android 设备在同一局域网中可通过 HTTP/mDNS 进行远程仓库验证。
- 大型远程仓库样本约 5225 个相册、267995 张图片，分页和缓存优化后可用。
- 远程断网后分类页隐藏离线远程相册，网络恢复后可重新连接。

这些结果来自手工验证，不等同于完整自动化回归。

## 主要风险

| 风险 | 影响 | 当前处理 |
| --- | --- | --- |
| release 未签名 | 无法作为正式安装包发布 | alpha 前配置签名 |
| 自动化测试不足 | 回归风险高 | 先保留 smoke test，后续补 Room/API/UI 测试 |
| API 36 尚未升级 | targetSdk 仍为 35，未验证 Android 16 行为变化 | 作为独立兼容任务升级 AGP/SDK 并执行回归 |
| 明文 HTTP | 局域网之外不安全 | 仅定位可信局域网 alpha 功能 |
| release 日志偏详细 | 可能泄漏路径/设备信息 | alpha 前降噪或在 release notes 中提示 |
| 后台与厂商限制 | 大仓库扫描/远程服务可能被系统限制 | 使用前台任务和日志定位，持续收集反馈 |

## 当前发布建议

首个发布已确定为 `0.1.0-alpha.3`，对应 `versionCode = 3`，计划使用 Git tag `v0.1.0-alpha.3`。

Alpha 发布定位：

- 面向作者和早期用户试用。
- 允许存在已知限制。
- 不承诺 Play Store 上架质量。
- 不承诺 SMB、视频、多用户、HTTPS 或公网访问。

## 下一步

1. 配置 release signing。
2. 完成 release notes，并处理或明确排除剩余本地 Git 改动。
3. 按[核心功能测试用例](../testing/07_测试用例.md)执行 Alpha Smoke Test，并使用[测试执行记录模板](../testing/测试执行记录模板.md)归档结果。
4. 打 `v0.1.0-alpha.3` tag 并发布 signed APK。
