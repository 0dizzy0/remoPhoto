# remoPhoto 0.1.0-alpha.4 候选回归记录

## 1. 基本信息

| 项目 | 记录 |
| --- | --- |
| 测试日期 | 2026-07-02 |
| 测试人员 | Codex（ADB 控制，维护者提供真机和签名环境） |
| 工作区 | 候选提交 `286f78f`；CI 修复提交 `c60c776`、`c770b3a` 已推送到 `master` |
| versionName / versionCode | `0.1.0-alpha.4` / `4` |
| 主测设备 | vivo V1981A，Android 12 / API 31，arm64-v8a |
| 升级来源 | 已安装的公开签名基线 `0.1.0-alpha.3` |
| 最终本地候选 APK | `app/build/outputs/apk/release/app-release.apk` |
| 候选 SHA-256 | `e8b413352c6e8d148bcf460a9d22093a55f482b03dce071c2adb7f3d31d1bd32` |
| 签名 | APK Signature Scheme v2，通过；与 `alpha.3` 为同一证书 |

本记录不代表 GitHub Release 已发布。GitHub Actions 首次远程运行已通过，最终发布提交、Tag、prerelease 和线上附件复验仍待完成。

## 2. 自动门禁

| 检查项 | 结果 | 证据 |
| --- | --- | --- |
| `:app:testDebugUnitTest` | PASS | 19/19，4 个测试类，0 failure/error/skipped |
| `:app:lintDebug` | PASS | `No issues found` |
| `:app:assembleDebug` | PASS | Debug 包使用 `com.remophoto.debug`，不覆盖 Release |
| `:app:assembleRelease` | PASS | 生成 signed `0.1.0-alpha.4` APK |
| `:app:connectedDebugAndroidTest` | PASS | 1/1，Room v3→v4 migration |
| GitHub Actions | PASS | run `28589376860`；JVM 测试、Lint 和 Debug 构建成功 |
| Release 签名 | PASS | v2 签名、单 signer |
| APK 元数据 | PASS | 包名 `com.remophoto`，minSdk 29，targetSdk 35，versionCode 4 |

## 3. 新增自动化覆盖

### 相册规则

- 分页覆盖负数、0、1、`P`、`P+1` 和跨多页边界。
- 名称定位覆盖 trim、忽略大小写、Unicode 和无匹配。
- 六种相册排序均验证主排序键和确定性名称/ID 次级顺序。
- 相册树子节点递归排序。

### 备份输入边界

- manifest 当前版本、非法版本、未来版本和数据库版本不一致。
- 无 manifest 的旧备份保持兼容。
- ZIP 条目白名单和 `settings/` DataStore 路径。
- 未知条目、路径穿越、重复条目、空 ZIP 和损坏 ZIP 拒绝。
- 常见 ZIP local/empty/spanned signature 识别。

### Room migration

真机测试创建 v4 数据库后按历史结构重建 v3 `albums` 表，写入两张相册和两张图片，再由 Room 执行 `MIGRATION_3_4`：

- 有图片相册的 `last_modified` 回填为最大图片修改时间 `20`。
- 空相册回填为 `0`。
- 数据库版本升级到 4。
- Room 最终 schema 校验通过。

## 4. 覆盖升级与 Smoke Test

| 场景 | 结果 | 说明 |
| --- | --- | --- |
| `alpha.3 → alpha.4` 同签名覆盖安装 | PASS | `firstInstallTime` 保持 `2026-06-28 16:48:15`，版本更新为 code 4 |
| 冷启动 | PASS | 升级后 287 ms；备份导入后 264 ms |
| 分类数据保留 | PASS | 升级前创建 `alpha4-upgrade-check`，升级后仍可见 |
| 分类清理 | PASS | 验证后删除，恢复“暂无分类” |
| 数据库导出 | PASS | 生成 2.58 KB Backup-v1 ZIP |
| 设置恢复 | PASS | 导出时高对比度开启，导出后关闭，导入并重启后恢复为开启 |
| Crash buffer | PASS | migration、升级、导出/导入和重启后均无新增记录 |

最终 SHA-256 对应 APK 在同版本重新安装后再次冷启动通过（299 ms），设置保持测试清理后的高对比度关闭状态，Crash buffer 仍为空。

导出的临时备份已从设备下载目录删除，测试分类已删除，高对比度已恢复为测试前的关闭状态。

## 5. 本轮裁剪

- 未修改远程协议、服务或缓存，因此引用 2026-06-28 双机远程回归，不重复完整双机测试。
- 未修改扫描、SAF 和图片加载；设备当前没有本地测试仓库，因此引用 `alpha.3` 本地核心回归，新增相册排序行为由 JVM 测试覆盖。
- 未升级 minSdk、targetSdk 或 AGP，不执行 API 29/35/36 专项。
- 数据库替换阶段故障注入和 Compose UI 自动化仍按风险安排，不阻塞本轮个人项目 Alpha。

## 6. 发布前剩余项

- [x] 审阅并提交当前候选变更。
- [x] 推送后确认 GitHub Actions 首次运行通过。
- [ ] 用最终 commit 重建 signed APK，并同步最终 SHA-256。
- [ ] 创建 `v0.1.0-alpha.4` Tag 和 GitHub prerelease。
- [ ] 重新下载线上 APK，核对 hash、签名、版本并完成冷启动。
