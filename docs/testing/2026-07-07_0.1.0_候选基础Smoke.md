# 0.1.0 候选基础 Smoke 记录

## 1. 结论

`0.1.0` 本地候选基础 Smoke 通过。候选 APK 已完成本地 JVM 测试、Android Lint、signed Release 构建、SHA-256、v2 签名、覆盖安装、版本校验、冷启动、UI dump、截图、Crash buffer、进程日志、应用文件日志、基础 Release 日志隐私扫描和内存快照采集。

同一主力真机随后执行 `connectedDebugAndroidTest`，Room v3→v4 migration 和数据库/设置导入失败回滚 2 条 Android 集成测试均通过。

本记录不替代正式发布前的手工专项：`beta.1 -> 0.1.0` 数据保留确认、有效备份恢复、备份流程 Release 日志隐私抽查和线上附件回下载复验需要专项记录承接。主力真机本地 SAF 核心、双机远程 Smoke、备份恢复和升级数据保留已在后续专项记录中补齐。

## 2. 环境与产物

| 项目 | 值 |
| --- | --- |
| 日期 | 2026-07-07 |
| Git 分支 | `codex/api29-saf-name-fix` |
| Git 基线 | `d820ed8`，工作区含 `0.1.0` 候选元数据和 Smoke 脚本改动 |
| APK | `app/build/outputs/apk/release/app-release.apk` |
| 版本 | `0.1.0`，`versionCode = 6` |
| APK SHA-256 | `e1a5b121c5daad75c16f8119aaf68887d311a3e0b5de566ec66b8cea4c45e7bf` |
| 主测设备 | vivo V1981A，Android 12 |
| 完整 Smoke 证据 | `.cache/qa/smoke/20260707-201823/` |
| Android 集成测试报告 | `app/build/outputs/androidTest-results/connected/debug/` |

本地截图、UI dump、logcat、Crash buffer、meminfo 和 Android 测试报告均位于 Git 已忽略目录，不提交设备截图或日志原文。

## 3. 自动门禁与基础 Smoke

执行命令：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\run-smoke.ps1 `
  -BuildRelease `
  -ExpectedVersionName "0.1.0" `
  -ExpectedVersionCode 6
```

| 检查项 | 结果 | 证据 |
| --- | --- | --- |
| `:app:testDebugUnitTest` | PASS | 一键 Smoke 中执行，完整 JVM 测试 24/24 |
| `:app:lintDebug` | PASS | 一键 Smoke 中执行，`No issues found` |
| `:app:assembleRelease` | PASS | 生成 signed Release APK |
| APK SHA-256 | PASS | `e1a5b121c5daad75c16f8119aaf68887d311a3e0b5de566ec66b8cea4c45e7bf` |
| Release 签名 | PASS | `apksigner verify` 通过 |
| 覆盖安装 | PASS | `adb install -r --no-streaming` |
| 版本校验 | PASS | `versionName=0.1.0`，`versionCode=6` |
| 冷启动 | PASS | `LaunchState: COLD`，`TotalTime=414 ms` |
| 进程存活 | PASS | 启动后 `pidof` 返回应用进程 |
| UI dump / 截图 | PASS | 已采集 `ui.xml` 和 `screenshot.png` |
| Crash buffer | PASS | 无新增崩溃 |
| 基础 Release 日志隐私扫描 | PASS | `content://`、`/sdcard/`、`/storage/`、IPv4、`FATAL EXCEPTION` 和 Crash buffer 包名命中均为 0；应用文件日志成功拉取 |

脚本增强后会生成 `privacy-scan.md`，并尝试拉取 `/sdcard/Android/data/com.remophoto/files/remoPhoto/logs`。完整 Smoke 记录中隐私扫描结果为：`contentUri=0 storagePath=0 ipv4=0 fatal=0 crashPackage=0 debugPriority=0 appFileLogLines=1460`。

## 4. Android 集成测试

执行命令：

```powershell
.\gradlew.bat :app:connectedDebugAndroidTest --console=plain
```

| 测试 | 结果 |
| --- | --- |
| `Migration3To4Test.migrationBackfillsAlbumLastModifiedAndMatchesRoomSchema` | PASS |
| `DatabaseImportRollbackTest.failureAfterSettingsRestoreRollsBackDatabaseAndSettings` | PASS |

结果摘要：vivo V1981A / Android 12 上完成 2/2 Android 集成测试，`BUILD SUCCESSFUL`。

## 5. 尚未覆盖的正式发布门禁

| 门禁 | 状态 | 下一步 |
| --- | --- | --- |
| `0.1.0-beta.1 -> 0.1.0` 数据保留 | 已完成 | 见 `2026-07-08_0.1.0_候选升级数据保留.md` |
| 有效备份恢复 | 已完成候选 | Android 集成测试覆盖成功导入；Release 包应用内导出和日志隐私见 `2026-07-08_0.1.0_候选备份恢复自动化.md` |
| 本地 SAF 核心 | 已完成 | 见 `2026-07-07_0.1.0_候选本地SAFSmoke.md` |
| 双机远程 Smoke | 已完成 | 见 `2026-07-07_0.1.0_候选双机远程Smoke.md` |
| Release 日志隐私完整抽查 | 已完成候选 | 基础启动、本地 SAF、双机远程和备份导出流程扫描通过 |
| CI 和线上附件复验 | 待执行 | 推送后确认 CI；GitHub Release 附件回下载后校验 hash、签名和版本 |

## 6. 发布判断

当前候选具备继续进入正式版发布执行的条件；尚不能公开发布，原因是 CI、Tag、Release 和线上附件复验仍未完成。
