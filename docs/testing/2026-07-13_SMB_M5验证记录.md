# SMB M5 验证记录

日期：2026-07-13

分支：`codex/smb-support`

基线：M4 提交 `c0c3d657160d0ea5b9c6b7c2fb76d49180f4d1a3`

状态：**进行中，尚未达到 M5 退出条件**

## 本轮结论

M5 已完成本地构建、两台真机 instrumentation、Windows SMB3、remoPhoto HTTP 双机、Release 升级与隐私、资源压力和已发现供应链漏洞的修复回归。当前无 Crash、ANR、数据库迁移失败或持续资源泄漏证据。

仍缺 API 29 SMB 运行时、Samba/NAS、完整异常矩阵以及单机无人值守 SMB Smoke。当前 Android SDK 没有 AVD，两台已连接设备分别为 API 31 和 API 36；局域网只有 Windows SMB 服务，因此这些缺项不能以现有结果替代。

## 自动化与构建

| 检查 | 结果 |
| --- | --- |
| `:app:testDebugUnitTest` | PASS，65/65，0 failures，0 errors |
| `:app:lintDebug` | PASS，No issues found |
| `:app:assembleDebugAndroidTest` | PASS |
| `:app:assembleRelease` | PASS，R8/minify 与 lintVital 通过 |
| API 36 instrumentation | PASS，12/12；Crash buffer 为空；无 last ANR |
| API 31 instrumentation | PASS，12/12；Crash buffer 为空；无 last ANR |

Instrumentation 覆盖 Room v3→v4、v4→v5、v4 备份导入与回滚、Keystore 存取替换删除、分页，以及 SMB 静态图/GIF 流释放、成功刷新远端删除和失败刷新保留上一完整索引。

## Windows SMB3 实机回归

设备：vivo V1981A / Android 12 / API 31，Debug 包。

- 使用设备中已保存的加密凭据，不读取或输出密码。
- 已有 SMB 仓库刷新后保持 115 个相册、8448 张图片和“已连接”状态。
- 基线实测完整扫描约 30.8 秒，500 条一批写入 16 批后写入余下 448 条；活动 session 最终归零。
- 本次 Bouncy Castle 1.84 覆盖后重新刷新，仓库仍为 8448 张图片且连接正常；Crash buffer 为空，无 last ANR。
- 测试前设备 Wi-Fi 为关闭；回归期间临时开启，结束后已恢复关闭。

## remoPhoto HTTP 双机综合回归

服务端：vivo V2254A / Android 16 / API 36。客户端：vivo V1981A / Android 12 / API 31。

- mDNS 自动发现通过，`/api/albums` 返回 HTTP 200，元数据为 5249 个相册、268871 张图片，响应约 1.39 MB。
- 客户端手动刷新约 926 ms，显示 263 页；进入相册同步 27 张图片并打开静态图成功。
- 强制停止服务端后，客户端稳定进入连接错误且保留旧索引；重启服务端后首次恢复探测即成功，原连接重新变为已连接。
- 连续 5 次刷新数量稳定。选择 102 张图片的相册连续横向浏览 100 张，无 Crash/ANR。
- 临时 HTTP 仓库已从 Debug 和 Release 数据中删除，原 SMB 与本地仓库均保留。

## 资源压力

HTTP 大索引刷新前后 PSS 从约 176177 KB 变为 172928 KB，线程均为 46。300 帧样本中 jank 6 帧（2%），P50/P90/P95/P99 分别约 6/9/15/26 ms，无 missed vsync。

连续浏览 100 张图片期间，PSS 峰值约 346919 KB，其中 Graphics 约 241228 KB；离开查看器 20 秒后 Graphics 降至约 195812 KB。应用进入后台并触发完整内存回收后，PSS 降至约 110433 KB、Graphics 约 11344 KB，热恢复正常。该证据更符合可回收图片缓存/图形内存压力，未观察到持续泄漏；不把单次 `meminfo` 超时当作应用崩溃。

## Release 升级与隐私

- 在 Android 12 既有 `0.1.0` 上同签名覆盖安装，`firstInstallTime=2026-06-28 16:48:15` 保持不变。
- 本地仓库 `download` 的 70213 张和 `RemoSmokeUi` 的 2 张图片完整保留。
- 最终 APK 大小 5802452 字节，SHA-256 为 `F75158B31DE66CFAE82CEFDA96AD16D70531B8240921DA06BA693B84D6EA171B`。
- 将四处高频布局诊断从 INFO 降为 DEBUG 后，Release 短流程新增日志由 289 行降为 36 行，布局日志命中为 0。
- 修复未加引号且包含空格/标点的 `name=` 字段只脱敏首段的问题，并增加回归测试；最终实际 Release 流程中 `name`、`repoName`、`displayName` 和 `fileName` 均完整替换为 `<redacted>`。
- 最终新增日志未命中 `content://`、存储路径、HTTP 地址、IPv4、`primary:`、原始名称、password、username 或 fatal；Crash buffer 为空，无 last ANR。

## 供应链与许可证

- SMBJ 保持 `0.14.0`，不纳入此前明确延期的上游 issue，也不改变 SMB 签名策略。
- Gradle 原解析 `org.bouncycastle:bcprov-jdk18on:1.79`。GHSA-c3fc-8qff-9hwx / CVE-2026-0636 标记 1.74～1.83 受影响、1.84 修复。
- 应用新增 dependency constraint 固定 `bcprov-jdk18on:1.84`；`dependencyInsight` 显示约束原因和 `1.79 -> 1.84` 冲突解析结果。
- 升级后完整 JVM/Lint/AndroidTest/Release、API 31/36 instrumentation、Windows SMB 刷新和 Release 覆盖安装均通过。
- SMB 新增运行时依赖的许可证记录在仓库根目录 `THIRD_PARTY_NOTICES.md`。完整既有依赖 SBOM 扫描仍需在最终发布门禁执行。

## 尚未完成的 M5 门禁

1. API 29 真机或 AVD 的 SMB 认证、中文目录枚举、图片流和 minified Release 运行时回归。
2. 独立 Samba/NAS 服务端互通；不得用同一 Windows SMB 服务器重复结果替代。
3. 汇总错误密码、SMB1-only、离线、共享移除、权限不足、超时、取消和恢复的完整异常矩阵；M4 已覆盖其中一部分，但 M5 需要统一候选版证据。
4. `scripts/` 下的无人值守 SMB Smoke：前置检查、临时共享、安装、添加、同步、静态图/GIF、断线恢复、增删刷新、删除仓库、UTF-8 证据目录和单一 PASS/FAIL 汇总。
5. 更新最终功能规格、测试用例、隐私说明和 `0.2.0` Release Notes，并在候选 APK 上复跑供应链与隐私门禁。

## 下一步建议

优先准备 API 29 AVD 和一个独立 Samba 容器/NAS 共享，先补齐兼容矩阵；两者通过后再固化无人值守脚本，因为脚本的错误分类和断言应以真实矩阵行为为准。该顺序不需要重构业务代码。如果无人值守脚本要求自动创建 Windows 本地账号/共享或管理 Samba 容器，则属于会改变开发机服务状态的操作，实施前应单独确认权限和清理策略。
