# v0.1.0 发布检查记录

## 1. 结论

`v0.1.0` 已于 2026-07-08 发布到 GitHub Releases。发布提交为 `aeca0beac686ed4e026a8ca65d3aa30f0d18f88d`，Tag 为 `v0.1.0`。线上 APK 与 `.sha256` 附件已回下载复验，hash、v2 签名和版本元数据均通过。

同版本线上 APK 的设备侧重复覆盖安装未计为通过：Android 16 设备返回 `INSTALL_FAILED_ABORTED: User rejected permissions`，Android 12 设备安装命令超时。线上 APK SHA-256 与本地已完成一键 Smoke、备份导出、Android instrumentation 和升级数据保留验证的候选 APK 完全一致，因此本次发布接受该设备重装限制并记录为残留操作风险。

## 2. 发布信息

| 项目 | 值 |
| --- | --- |
| Release | `v0.1.0` |
| GitHub Release | `https://github.com/0dizzy0/remoPhoto/releases/tag/v0.1.0` |
| Commit | `aeca0beac686ed4e026a8ca65d3aa30f0d18f88d` |
| CI | Android CI #13，通过 |
| APK | `remoPhoto-0.1.0.apk` |
| SHA-256 | `aa3a392eb1085008be9a16260419b80b18fd3326683eb179f399300d78c56eb1` |
| 签名证书 SHA-256 | `cf3728348cbd505ecd33f7ee5be89eb51326d4c721dda76a8d7da45a066ea7b9` |
| 线上复验证据 | `.cache/release-verify-v0.1.0-online/summary.md` |

## 3. 发布执行

| 步骤 | 结果 |
| --- | --- |
| 合并 `codex/api29-saf-name-fix` 到 `master` | PASS，fast-forward 到 `aeca0be` |
| 推送 `master` | PASS |
| GitHub Actions | PASS，Android CI #13 |
| 创建并推送 `v0.1.0` Tag | PASS |
| 创建 GitHub Release | PASS |
| 上传 APK 与 `.sha256` | PASS |
| 回下载 APK 与 `.sha256` | PASS |
| 回下载 hash 校验 | PASS |
| 回下载 APK 签名校验 | PASS，APK Signature Scheme v2 |
| 回下载 APK 版本校验 | PASS，`versionCode = 6`、`versionName = 0.1.0` |
| 回下载 APK 同版本重复覆盖安装 | 未通过设备侧执行，见第 4 节 |

## 4. 设备重装结果

- Android 16 / iPA2575：线上 APK push 成功；`pm install -r`、`pm install -r -g` 和 install session commit 均返回 `INSTALL_FAILED_ABORTED: User rejected permissions`。设备保留已安装 `0.1.0 / versionCode 6`，force-stop 后冷启动 `258 ms`，Crash buffer 为空。
- Android 12 / vivo V1981A：线上 APK push 成功；`pm install -r` 超时，与该设备前序安装卡住行为一致。已停止残留 adb 安装进程，设备保持在线。

## 5. 关联候选证据

- `.cache/qa/smoke/20260708-230348/summary.md`
- `.cache/qa/androidTest-20260708-2310.txt`
- `.cache/qa/backup-flow/20260708-2315/summary.md`
- `.cache/qa/upgrade-beta1-to-010/20260708-android16/summary.md`
