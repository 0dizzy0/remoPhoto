# remoPhoto 0.1.0-beta.1 Release Notes

> 当前状态：候选验证收尾中，尚未发布。主力真机和双机远程门禁已通过；最终 APK、Tag 和线上附件一致性复验完成前不发布。

## 本次重点

`0.1.0-beta.1` 冻结大功能，集中验证从公开 Alpha 升级、备份恢复、数据完整性、远程稳定性和 Release 日志隐私。

## 主要变化

- 版本递增为 `versionCode = 5`、`versionName = 0.1.0-beta.1`。
- 固定相册分页、稳定排序、Unicode/空白名称和模糊定位行为。
- 固定备份 manifest/version、条目白名单、重复条目、路径安全和损坏 ZIP 的拒绝规则。
- 增加 Room v3→v4 migration 和备份导入失败回滚的可重复 Android 集成测试。
- 保留最小 GitHub Actions CI，对 JVM 测试、Android Lint 和 Debug 构建执行持续验证。

## 发布前验证

- [x] CI、JVM 测试、Lint 和 signed Release 构建通过。
- [x] `0.1.0-alpha.4 → 0.1.0-beta.1` 同签名覆盖安装，数据库与设置保留。
- [x] 有效备份导出和恢复通过；非法输入拒绝与失败回滚证据有效。
- [x] 主力真机完成本地核心、冷启动和 Crash buffer 检查。
- [x] 两台真机完成发现、连接、分页、图片、缓存、离线和恢复回归。
- [x] Release 日志不包含 DEBUG、完整路径、URI、IP、设备名或用户自定义名称。
- [ ] APK 版本、签名、SHA-256、Tag、Release Notes 和线上附件一致。

## 安装与升级

- 当前公开版本仍为 `0.1.0-alpha.4`；Beta 完成门禁后将在 [GitHub Releases](https://github.com/0dizzy0/remoPhoto/releases) 提供 `remoPhoto-0.1.0-beta.1.apk` 和对应 `.sha256` 文件。
- 已验证从公开 `0.1.0-alpha.4` 直接覆盖升级，数据库、分类和设置保留。升级前仍建议使用应用内“导出数据库”创建备份。
- 更早的内部 Alpha 2 使用不同签名，不能直接覆盖；应先导出备份，再安装公开签名版本并导入。
- 下载后可在 Windows PowerShell 中执行：

```powershell
Get-FileHash .\remoPhoto-0.1.0-beta.1.apk -Algorithm SHA256
Get-Content .\remoPhoto-0.1.0-beta.1.apk.sha256
```

两个值必须完全一致。首次安装需要允许当前文件管理器或浏览器“安装未知应用”，安装完成后应关闭该临时授权。不要从第三方 APK 站点下载。

## 验证摘要

- JVM 测试 19/19，Android 真机集成测试 2/2，Android Lint `No issues found`。
- vivo Android 12 完成 `alpha.4 → beta.1` 覆盖升级、本地核心、有效备份恢复和导入失败回滚。
- vivo Android 16 / Android 12 双机完成 mDNS、5249 个相册、268871 张图片、263 页分页、图片缓存、离线和恢复回归。
- 最终本地候选 APK SHA-256 为 `c4af2efe86786016d6179bac6e0a916146dab6a17eaa6f02a85801bd16aaec69`；精确产物已覆盖安装到两台设备，冷启动分别为 `197 ms` 和 `305 ms`。
- Release 进程日志未发现 DEBUG、完整路径、URI、IP、设备名或用户自定义名称；Crash/ANR 为 0。

## 已知限制

- 远程服务使用无认证明文 HTTP，只适用于可信局域网。
- targetSdk 为 35；Android 16/API 36 已通过覆盖安装、冷启动和远程服务基础验证，更完整兼容性专项安排在正式版前。
- SMB、视频、多用户和完整 UI 自动化不属于本次 Beta 范围。
