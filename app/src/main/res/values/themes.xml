<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 启动屏主题（使用 SplashScreen API） -->
    <style name="Theme.RemoPhoto.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.RemoPhoto</item>
    </style>

    <!-- 基础主题（会被 Compose 主题覆盖） -->
    <style name="Theme.RemoPhoto" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
